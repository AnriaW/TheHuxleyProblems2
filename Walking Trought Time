#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    float A, B, C;
    
    scanf("%f %f %f", &A, &B, &C);
    
    if( A!=B && A==C || A!=C && A==B || A!=B && B==C || A==B && B==C ){
        printf("S\n");
    }
    else if (A==B+C || B==A+C || C==A+B){
        printf("S\n");
    }
    else {
        printf("N\n");
    }
}
