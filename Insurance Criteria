#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int sex, age;
    scanf("%d %d", &sex, &age);
    
    if (sex==0 && age==1)
    {
        printf("1");
    }
    else{
        printf("0");
    }

	return 0;
}
