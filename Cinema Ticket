#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int est, ido;
    scanf("%d %d", &est, &ido);
    
    if(est==0 && ido==0){
        printf("0");
    }
    
    else{
        printf("1");
    }
	return 0;
}
